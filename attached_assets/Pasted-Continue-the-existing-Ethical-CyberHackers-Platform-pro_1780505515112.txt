Continue the existing Ethical CyberHackers Platform project.

Task:
Infrastructure Stabilization — Supabase Status Verification + Local-First Backend Alignment

Purpose:
Verify the current Supabase backend status first, then update only the infrastructure pieces needed to align the app with the planned backend foundation while preserving the local-first architecture.

Do NOT add Assignment 4.
Do NOT redesign UI.
Do NOT change gameplay flow.
Do NOT remove localStorage.
Do NOT make Supabase mandatory for gameplay.
Do NOT require full student login before gameplay.
Do NOT break Assignments 1–3.

---

## CRITICAL RULE

The platform must remain local-first.

localStorage remains the primary source of truth for immediate gameplay.

Supabase is only:

* background sync
* persistent backup
* analytics foundation
* future teacher/classroom foundation

If Supabase fails:

* gameplay continues normally
* progress still saves locally
* no student-facing crash occurs

---

1. VERIFY SUPABASE STATUS ONLY FIRST

---

Before changing code, verify and report:

1. Are these Replit Secrets present?

   * SUPABASE_URL
   * SUPABASE_ANON_KEY

2. Is the Supabase client initialized safely?

3. Does the project connect successfully to Supabase?

4. Do these tables exist in the connected Supabase project?

   * profiles
   * missions
   * student_progress
   * mission_attempts
   * xp_events
   * certificates

5. Are RLS policies present and not blocking expected anonymous/local-first sync?

6. Are migrations present locally?

   * supabase/migrations/001_initial_game_schema.sql

7. Is backendSync.js currently using old table names or the new schema?

8. Does the app currently show:

   * Backend: Local Only
   * Backend: Supabase Connected
   * Backend: Sync Delayed

After verification, create/update:

docs/SUPABASE_STATUS_REVIEW.md

Include:

* Connected / Not Connected
* Tables Found / Missing
* Secrets Found / Missing
* Current sync layer status
* Risks
* Recommended next action

---

2. UPDATE SUPABASE SCHEMA ALIGNMENT

---

If the database schema exists, align the app sync layer to the current schema.

Expected tables:

* profiles
* missions
* student_progress
* mission_attempts
* xp_events
* certificates

Do NOT create new table names unless absolutely necessary.

Do NOT revert to older B0 table names.

Do NOT manually create fake local-only tables.

---

3. PRESERVE SIMPLE LOGIN / ANONYMOUS MODE

---

Do NOT implement full authentication yet.

Keep current simple player identity flow.

Use:

* display_name
* anonymous_id
* optional user_id nullable for future auth

Requirements:

* anonymous_id generated once
* stored in localStorage
* reused across sessions
* mapped to profiles.anonymous_id
* no forced email/password login
* no Google login
* no school login yet

Authentication is future-facing only.

The profiles table may support auth.users through nullable user_id, but gameplay must not require auth.

---

4. PERSISTENT PROGRESSION ALIGNMENT

---

Connect local progress to Supabase background sync.

Use student_progress for:

* current/best mission status
* mission completion
* best score
* best confidence
* highest XP earned
* attempts count
* last played
* completed_at

Rules:

* localStorage updates first
* Supabase sync happens second
* failed sync does not block gameplay
* retries can happen quietly

---

5. MISSION ATTEMPT TRACKING

---

Use mission_attempts for replay history.

Rules:

* every restart creates a new attempt
* old attempts are never overwritten
* attempt_number increments per anonymous_id + mission_id
* completed attempt stores scorecard_json
* status is tracked as started/completed/abandoned where appropriate

Restart Assignment must NOT erase full user history.

Clear Progress may erase/reset local journey only after explicit user action.

---

6. XP / RANK SYSTEM ALIGNMENT

---

Keep current XP/rank behavior visually unchanged.

Update backend sync so XP and rank-related progress are persisted correctly.

Use:

* profiles.xp_total
* profiles.current_role
* profiles.analyst_reputation
* profiles.promotion_readiness
* xp_events for individual XP changes

Rules:

* XP updates locally first
* xp_events logs meaningful XP events
* avoid logging excessive tiny events
* mission completion XP must be tracked
* major reasoning/evidence milestones may be tracked

Do NOT redesign XP UI.

---

7. MISSION SCORING ALIGNMENT

---

Preserve current scoring behavior.

Ensure Supabase can store:

* mission outcome
* trust score change
* analyst confidence
* evidence confidence
* containment/readiness score
* final threat level
* scorecard_json

Use:

* student_progress for best/current result
* mission_attempts for each attempt record

Do NOT change scoring logic unless needed to fix sync.

---

8. SAVE / LOAD SYSTEM STABILIZATION

---

Preserve:
localStorage-first save/load.

Add or stabilize:

* saveCloudProgress()
* loadCloudProgress()
* syncPlayerProfile()
* syncMissionProgress()
* startMissionAttempt()
* completeMissionAttempt()
* trackXpEvent()

Load behavior:

1. Load localStorage first.
2. If Supabase is connected, compare available cloud progress.
3. Do not automatically overwrite local progress unless clearly newer or explicitly safe.
4. Avoid destructive merges.
5. Log warnings instead of crashing.

---

9. BACKEND STATUS INDICATOR

---

Ensure backend status indicator is accurate:

* Backend: Local Only
* Backend: Supabase Connected
* Backend: Sync Delayed

It should reflect real status.

Do not make it visually intrusive.

---

10. ERROR HANDLING

---

All Supabase calls must be safe.

Requirements:

* try/catch around sync calls
* no uncaught promise failures
* no student-facing scary errors
* console warnings acceptable
* gameplay continues even if backend fails

---

11. VERIFICATION AFTER CHANGES

---

After updates, verify:

Assignment 1:

* start
* complete
* XP update
* scorecard
* localStorage save
* Supabase sync attempt

Assignment 2:

* same checks

Assignment 3:

* same checks

Backend:

* profile row can sync
* student_progress row can sync
* mission_attempt row can sync
* xp_event can sync
* app still works if Supabase is unavailable

---

12. OUTPUT REPORT

---

Create/update:

docs/INFRASTRUCTURE_STABILIZATION_REPORT.md

Include:

* what was verified
* what was changed
* what remains local-first
* what syncs to Supabase
* what authentication is deferred
* any risks
* exact test results
* next recommended backend step

---

## FINAL GOAL

Stabilize infrastructure for:

* Supabase schema
* future authentication
* persistent progression
* XP/rank system
* mission scoring
* save/load system

while preserving the existing local-first game architecture and keeping Assignments 1–3 fully playable.
