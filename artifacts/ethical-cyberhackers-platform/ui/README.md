# /ui

Place reusable UI component files here as the app grows.

Examples:
- modal.js       — reusable modal/dialog component
- toast.js       — notification toasts
- progress.js    — XP progress bar component
- sidebar.js     — collapsible sidebar
