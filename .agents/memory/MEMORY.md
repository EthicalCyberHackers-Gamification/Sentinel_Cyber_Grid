- [Operations Center home re-render](ech-landing-render.md) — every path that reveals `#moduleLanding` must call `renderOperationsCenter()` or it shows stale progress.
- [OC home title resolution](ech-oc-home-title-resolution.md) — generic missions 004-006 read the ENGINE title, not MISSION_MAP; align home titles via OCV2_GENERIC_DISPLAY override (MISSION_MAP has legacy Object.keys mini-map loops; missions.js = interior).
- [Ops Center V2 three-panel layout](ech-ocv2-layout.md) — graduated from prototype; ocv2-* classes in style.css, initOcv2()/renderOcPanelV2() in script.js called from renderOperationsCenter(); preserved IDs live in .ocv2-compat-hidden div.
- [Collapsible + resizable side columns](ech-collapsible-columns.md) — presentation-only widen-center collapse/drag-resize layer (key `ech.ui.v1`); gotchas live in the topic file.
- [e2e localStorage frame mismatch](ech-e2e-localstorage-frame.md) — can't prime localStorage from the harness (top frame ≠ app iframe); test restore by driving the app UI then reloading.
- [Sim notebook = shared M1–M4 surface](ech-sim-notebook-shared-surface.md) — right column (notebook+feedback) is the shared judgment/completion surface & syncDecisionDock chokepoint; relocating it is cross-cutting M2–M4 work.
- [Active-dashboard viewport lock](ech-active-dashboard-viewport-lock.md) — `body.mission-running .dashboard` (overflow:hidden + grid auto 1fr) clips tall content/forces zoom; let the PAGE scroll during active play.
- [Supabase backend (local-first)](ech-supabase-backend.md) — optional cloud mirror; localStorage stays authoritative, sync never throws/blocks, attempts resume-safe, completion idempotent across both completion paths.
- [Hoverable locked cards](ech-locked-card-tooltips.md) — `disabled` buttons fire no hover/focus events; lock M2/M3 command cards with `aria-disabled` + click guard so their tooltips still work.
- [Command execution asymmetry](ech-command-runner-asymmetry.md) — M1 is text-parser-driven; M2/M3 are key-driven (`.cmd` per key). Typed-terminal features must reverse-map text→key, then call the existing key runner.
- [Gate pacing resume-safety](ech-gate-pacing-resume-safe.md) — commit reasoning-gate completion (answered/confidence/unlock/save) synchronously; defer only the cosmetic "Reviewing…" reveal + pin, never persistence.
- [State-aware hints](ech-state-aware-hints.md) — re-sync gate-dependent hints at the state-change site (e.g. `updateBriefingGate`), not just boot/reset; `loadCommandToTerminal` is the one chokepoint for "command loaded" guidance.
- [Completion chokepoint](ech-completion-chokepoint.md) — `notifyAssignmentComplete(missionId)` fires once across all completion paths/missions; hook cross-mission completion side-effects there (defer meter-touching ones; M1 re-renders threat right after).
- [Full e2e exceeds runTest budget](ech-e2e-runtest-timeout.md) — a whole-game A1→A3 runTest times out the 600s sandbox; scope tests small or verify cosmetic changes via node --check + typecheck + screenshot + clean console logs.
- [Cloud progress restore (faithful snapshot)](ech-cloud-restore-snapshot.md) — restore replays the full localStorage blob from an append-only table; reconcile by progression score so restore only raises progression; reload-once guard must self-clear; anon RLS is a bearer-capability model (isolation = deferred auth).
- [Briefing Replay layer (rgb*)](ech-briefing-replay.md) — presentation-only briefing recap chaining into spotlight replay; never persist/XP, only inert flag; any on-demand overlay must igTeardown() first or it stacks on a live spotlight.
- [Supabase migration apply/verify](ech-supabase-migrations.md) — drift gotcha: a migration's effects can exist while it's unrecorded in remote history; trust `migration list`, reconcile with idempotent `db push`.
- [Replay vs live spotlight exclusivity](ech-replay-vs-live-spotlight.md) — separate dim/coach ids aren't enough; on-demand replay must both guard igShow AND igTeardown() any live overlay on start (they share `.ig-spotlight-target`).
- [Persistence chokepoint](ech-persistence-chokepoint.md) — `saveProgress()` is the one place that writes `ech.progress.v1` AND enqueues cloud sync (and `awardXP`→`saveProgress`); presentation-only features must reach neither (briefing-review path persists — don't reuse for replay).
- [Live SOC Console interior](ech-soc-console.md) — prototype `sc*` terminal+reactive-map interior parallel to the holotable; gated per-mission on a `console:{}` block; SVG nodes need setAttribute('class'), not `.className =`.
- [Evidence Holotable interior](ech-holotable-interior.md) — prototype-only data-driven mission interior in ops-center-prototype; in-memory only (no localStorage), additive via HOLOTABLE_MISSIONS; guard timed effects with htRunToken across open/return/replay.
- [OCV2 severity casing](ech-ocv2-severity-casing.md) — `OCV2_NODE_META.severity` is UPPERCASE; node CSS classes/JS lookup maps are lowercase — `.toLowerCase()` before any keyed lookup or it silently hits the fallback.
- [Node tests for DOM-coupled JS](ech-node-test-pattern.md) — script.js runs DOM init on import (not node-loadable); extract pure logic to a sibling module, test that at runtime + grep its wiring.
- [Ops Center map projection](ech-ops-map-projection.md) — the Global Ops Map (both apps) is an exact equirectangular plate carrée (viewBox 1000x500); compute node/arc/land geometry from lat/long, don't eyeball it.
- [pnpm lockfile churn](pnpm-lockfile-churn.md) — temp pnpm add/remove rewrites pnpm-lock.yaml (strips catalogs, adds tarball); restore via `git show HEAD:pnpm-lock.yaml > pnpm-lock.yaml`.
- [Generic mission engine (M4/5/6)](ech-generic-mission-engine.md) — LEGACY/bypassed: 003-006 now route to the Progressive Lab; `GENERIC_MISSIONS` only supplies `quiz.xpReward` now.
- [Progressive Lab dataset schema](ech-lab-dataset-schema.md) — 003+ are per-file ES modules spread into LAB_MISSIONS; runner routes by cmd FIRST WORD only (no shared first words); verify via node validator, terminal is gated.
- [Career ladder (authoritative rank)](ech-career-ladder.md) — rank derives from the PERSISTED earned campaign role (not completion count); home + in-sim displays must share that one source or they diverge.
- [Shared Analyst Notebook render](ech-shared-notebook-render.md) — career-sim renderEvidencePanel is ONE shared path for M1–M4; gate new sections on a per-mission SIM.def flag; all four now branch into the caseFileNotebook two-step (observation→justification) judgment engine.
- [Notebook = live comms, not exam](ech-notebook-comms.md) — career-sim judgment surface is a Sarah Reyes comms thread; never render unchosen/keyed options; all feedback decoration is display-only from SIM state; "no correctness wording" needs DATA edits, not renderer.
- [World Continuity layer](ech-world-continuity.md) — presentation-only recurring-NPC/resolved-trace/bulletin flavor read from `missionMapStatus()`; intel feed must guard static items on a static marker, not children.length.
- [Progressive Lab graduation](ech-lab-graduation.md) — terminal lab graduated prototype→shipping via configureLab() hooks; ANY alternate lab entry (e.g. `?lab=`) must reuse the host gate + completion prereq guard or it bypasses unlock/onboarding.
- [Career progression (prototype)](ech-career-progression.md) — role/promotion derived read-only from completed-mission count; never persisted; promotion notice via session-only last-seen tier; bar uses in-mission confidence; framing labels only.
- [Lab intel cards](ech-lab-intel-cards.md) — presentation-only hover/focus/tap overlay on the lab map/rail; one viewport-clamped fixed card; touch needs a non-pinning info hotspot; SVG links need a wide hit-line + focusable midpoint marker.
- [Lab command verb routing](ech-lab-verb-routing.md) — progressive lab (`lab.js`, `?lab=mission-001`) routes on the FIRST typed word; key `LAB_VERB` on that word (e.g. `submit`→report), not the tool name, or multi-word cmds dead-end.
- [Orientation guided-tutorial panel](ech-lab-file-cmd-coaching.md) — Assignment 000 left-dock step list + TOP 6-stage workflow bar (not a dock tracker); completion keys on intended outcome, not the verb typed.
- [oc.js ↔ lab.js bridge](ech-lab-console-bridge.md) — separate ES modules, no shared scope; lab publishes `window.openMission001Lab`, `launchWorkspace` calls it (early-return before console/holotable) with a deep-link fallback.
- [Lab teaching surfaces](ech-lab-gradual-hints.md) — Mission-001 lab hints/explain-popups/tool-kit are presentation-only; 3-tier hints never leak the command early; bind persistent-host modal listeners ONCE in init, not per open.
- [Early-beginner support tier](ech-lab-beginner-support.md) — `support:{beginner:true}` per-mission gate (via `labSupportV2()`) swaps the command dock for a suspicion/question panel + analyst feedback; branch on the flag not a mission id; HINT is the only command reveal.
- [CyberCorp identity layer](ech-cybercorp-identity.md) — prototype-only immersion: CYBERCORP_IDENTITY + OP_CONTEXT(keyed by opId) drive identity panel, pre-mission briefing, mission-header context; recurring Reyes + J. Okafor.
- [Persistent world (Phase 3 prototype)](ech-persistent-world.md) — read-only continuity layer; mission-link edges must point to an EARLIER and SAME-ACTOR mission (first op of any actor gets no `connects`).
- [Orientation lab (Assignment 000)](ech-orientation-lab.md) — standalone `?lab=mission-000` beginner tutorial; isolation = absent from MISSION_PLAY_ORDER (notifyLabComplete no-ops) + canOpen bypass; engine reuse data-gated on `def.report.choices`, never branch shared fns on a mission id.
- [Orientation network map](ech-orientation-map.md) — gated on `def.topo.zones`/`mapReact`; suspicious link reddens at `trust.source` offbaseline+; calm/benign = single request dot, probing one-way inbound; fixed Network Inspector in orientation.
- [Lab stage-entry hooks](ech-lab-stage-entry-hooks.md) — five canonical forward-stage-entry sites (openLab/grep-aha/revealCampaign/dispatch fromStage-guard/unlockContainment); hook all five for once-per-stage presentation-only overlays.
- [Lab step coach (labGuide)](ech-lab-step-coach.md) — engine-level `→ Next:` direction for stages 2–5 (stage 1 uses dataset hooks); "remaining tools" must filter by run.discover's target group, not `unlock<=stage`.
- [Analyst Learning Layer (prototype lab M001)](ech-learning-layer.md) — presentation-only teaching layer gated on `learning.enabled`; a checkpoint that gates a functional unlock must resolve on EVERY dismissal path (one chokepoint + done flag) or it soft-locks.
- [Lab learning layer (explain + debrief)](ech-lab-learning-layer.md) — presentation-only glossary+SOC-debrief gated behind per-mission `learning.enabled`; strict gate means router/openGlossary must check `labLearning()`, not just hide chips.
- [Canonical mission title/severity source](ech-mission-title-source.md) — career-sim/`COURSE_MISSION_META` owns 001–004 titles+severity; legacy registry mislabels slot 4 as "Reconnaissance Sweep" (it's Data Exfiltration) — don't trust the registry.
- [opengraph.jpg worktree churn](ech-opengraph-worktree-churn.md) — a stray `public/opengraph.jpg` (either app) often shows modified and pollutes the review diff; check `git status` and restore via `git show HEAD:path > path` before finishing.
- [Campaign continuity ("the company remembers")](ech-campaign-continuity.md) — career-sim cross-mission memory; pure fns in career-dynamic.js, data-gated per-mission def.* keys, ONE additive idempotent write (companyHistory); referenced flags/evidence-ids must all resolve or it silently no-ops; companyTimeline only renders REAL mission-id keys.
- [Optional Side-Trails (earned-only)](ech-side-trails.md) — presentation-only side-trails/red-strings/foreshadow layer; reward = two flat setMissionFlag keys + Case-Board node ONLY (never confidence/scoring); data-gated per def.sideTrails/foreshadow + node sideTrailReveal/redString; bonus nodes excluded from device-count.
- [Career-sim dynamic conditions](ech-career-dynamic-conditions.md) — prior-mission carry-flags reshape a later mission via non-mutating buildEffectiveDef computed once on open; deltaMods folded POST-HOC at both decision sites; a `reveals:` id with no evidence def silently no-ops.
- [Career simulator integration](ech-career-sim-integration.md) — prototype career sim ported as isolated module: de-self-boot+re-hydrate, window-bridge coordination, saveProgress() persistence, lab.css supplies its palette, host-rendered home chrome (resbar + identity card).
- [oc.js home flavor drift](ech-oc-home-flavor-drift.md) — prototype home cards + alerts/comms/intel/ticker/bulletins/THREAT_ACTORS/WORLD_CONTINUITY are authored independently of sim.js; realign ALL together on mission changes and grep opId cross-refs.
- [Sim reactive Network Map](ech-sim-network-map.md) — career-sim device-map popup gated on `def.map`; now INTERACTIVE — node clicks reuse the existing graded setters (identify/classify), no new scoring/persistence; only player's own pick highlighted (no-spoiler); overlay on body OUTSIDE #careerOps.
- [Career-sim evidence layers](ech-career-evidence-layers.md) — beginner/analyst/technical `e.layers` via `evLayers()` label-fallback; engine scores only label/qualityWeight; presentation-only.
- [Career-sim command missions](ech-career-command-missions.md) — one engine runs file-model + command-model missions; routing exact-match-first then space-prefix; gating reads `ocp.career.v1` read-only.
- [Judgment-to-Power system](ech-judgment-to-power.md) — transient "earned tools" layer; confidence cost is DISPLAY-only (never the graded value); earn triggers must use a signal present in all 4 missions (observations, not M1-only classification); never reveal answers; the shared single `confSpend` can't stack — route recovery by `confSpendSource`; snapshotted view-state must also be rendered + stay re-committable.
- [Lab topo node refs](ech-lab-topo-refs.md) — map renderer silently drops links/mapReact for node ids missing from `topo.nodes` (no crash, broken map); clone the FULL node set; `tests/lab-topo-refs.test.js` guards it.
- [Consequence reactions: posture not correctness](ech-consequence-loop.md) — decision-reaction layers must key on action POSTURE (disruptiveness), never verdict, or they leak the answer; best-effort router needs try/catch + persist only on real mutation (or all-flags-off diverges on storage bytes).
- [Posture-signal correctness leak](ech-posture-signal-leak.md) — a "posture" recap signal can silently call a correctness helper (`*Count()` filtering by `*Correct`); trace the call not the name, prefer raw engagement state, and audit the feature slice for correctness-helper names.
- [Notebook re-render resets scroll](ech-notebook-scroll-rebuild.md) — renderEvidencePanel rebuilds the whole panel every interaction; capture+restore `.sim-evidence-body.scrollTop` (no `#simEvidenceBody` id exists) or it snaps to top on submit; focus needs preventScroll.
- [M1 grep triage (file-model)](ech-m1-grep-triage.md) — a triage tool that surfaces evidence must make files classifiable too (decouple "classifiable" from "deep-read"); reuse surfaceEvidence (never a new graded path); containment match not bidirectional.
- [M1 guided-hunt guidance](ech-m1-guided-hunt.md) — terminal-action nudges ("type X", "→ Next:") must be lock-aware: latch while the dock locks + flush from the unlock chokepoint (unlock handoff suppresses completion, so it's the only re-emit path); one beat speaks per frame.
- [Decision Dock modes](ech-reconsideration-pivot.md) — shared dock, 3 prioritized modes (graded call→reconsideration→finding draft) via prefixed _dockActiveId; BLOCKING add-ons join caseFileDecisionPending, NON-blocking (finding draft) stay OUT + gate focus-pull on decisionLocked(); reuse notebook card markup for free delegation; never grade/persist.
- [Command-brief tool keying](ech-command-briefs.md) — Guided-Terminal "new tool" brief keys on the TYPED verb, not the entry's match[0] (one entry can alias cat→tail); cmdBrief:* outside CANON_FLAGS.
- [Notebook navigation chrome](ech-notebook-nav-chrome.md) — presentation-only Focus overlay (frees notebook from static 1fr/1fr column) + kind-keyed collapse + status chips via applyNotebookChrome; run chrome BEFORE scroll restore; gate by section KIND not mission id.
- [objectiveTrack predicates](ech-objectivetrack-predicates.md) — progressive-objective doneBy needs ev:/flag:/challenge: prefix; fails closed (bare evidence id silently never ticks), unlike recommendation doneBy bare ids.
- [Decision-gate hard-lock](ech-decision-hard-lock.md) — gating only the terminal input leaks via early `decide` (action buttons bypass it); the pending predicate must be stage-independent (except `report`) and shared by EVERY action entry point; don't double up aria-live.
- [Terminal per-command output grouping](ech-terminal-command-grouping.md) — each command's echo+results wrap in `.sim-term-group` (green left accent); every non-command/system print must close the group first or it's trapped; sink self-heals on clear, close only on unlock/flush EDGES.
- [Case-file question rendering & cue ownership](ech-casefile-question-rendering.md) — replies authored correct-first, graded by option id NOT position (display order shuffled stably); terminal "In the notebook →" judgment cue suppressed while a Decision Dock call is pending.
- [Mission-1 flags rollout policy](ech-m1-flags-rollout.md) — generalize pacing-only `reviewBeforeCall` to all case-file missions; keep `uiComplexityLevel:'simple'` declutter + `grepTriage`/onboarding M1-only (they'd strip M2-M4 curriculum).
- [Career-sim 3-gauge performance bar](ech-career-3-gauge-bar.md) — top bar shows 3 composite red/yellow/green pointer gauges, but the 6 underlying resources still exist & still drive verdict + Mission 4 review; animate via in-place pointer update, not innerHTML rebuild.
- [Reactive home Active Threats chip](ech-reactive-threat-level.md) — OCV2 header "ACTIVE THREATS" (renamed from "THREAT LEVEL" to not contradict the Threat Defense gauge) is DERIVED (containment = ½ resolved/total + ½ Threat Defense), presentation-only from renderOperationsCenter; drive resolved from deriveCareerState; label text lives in index.html + render path.
- [Mission intro video cutscene](ech-mission-intro-cutscene.md) — plays EVERY launch, presentation-only, gated in launchMissionFromMap; break the finish→re-enter loop with a transient one-shot proceed flag (not a seen-key); m1IntroActive stops open-behind-overlay; ducks soundtrack.
- [Career verdict scoring & gauges](ech-career-verdict-scoring.md) — tier is skill-driven; standing capped so it can't cross Approved; gauges move only via resource deltas; weak multiplier shrinks business cost so BI needs its own penalty.
- [Transient overlay timer clobbering](ech-transient-overlay-timer.md) — auto-dismiss overlays must remove their OWN captured element (el.remove), never a global by-id remove, or a newer instance gets killed by an older timer.
- [Mission-complete result overlays](ech-completion-victory-modal.md) — result UI mounts on body OUTSIDE #careerOps so wire buttons directly (delegate won't catch them); tear down at the 3 completion-lifecycle sites; gate on a def flag not a mission id.
- [Shared-accessor behavior drift](ech-shared-accessor-drift.md) — a flag-gated one-mission feature still regresses others if it reroutes a SHARED reader through a new accessor; gate the BEHAVIOR change + return the exact prior expression in the else.
- [Multi-select draft key](ech-multiselect-draft-key.md) — SIM.discoveryDrafts keys by ch.id STRING; passing the challenge object silently keys "[object Object]" (empty, no throw) → chips never light, SEND stays disabled.
- [Passive Case Board + classification-in-dock](ech-case-board.md) — M1 investigation-game: self-building passive board (no click-to-place), classification relocated to dock as a NON-blocking card (still setClassification, never decisionLocked), de-quizzed chat dock; readiness bar = coverage-only (evidenceQuality), never classificationQuality/verdict; all def.*-gated so M2–M4 byte-identical.
- [Case-file reviewOnly files + no-spoiler risk wiring](ech-casefile-reviewonly-risk-wiring.md) — classify scorecard counts classifiableFiles() only, investigated-checks use fileInvestigated; a conclusion-risk's triggeredBy must be ONLY its confirming beat (careful copy alone won't stop the leak).
- [Career-sim mission-lifecycle DOM attrs](ech-career-stage-attr-lifecycle.md) — per-mission attrs/classes on #careerOps must be cleared in returnFromCareerMission(); the renderer's no-SIM.def clear path never fires on exit (SIM.def persists).
